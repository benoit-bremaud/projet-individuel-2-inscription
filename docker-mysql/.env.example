# Copier en docker-mysql/.env (NON versionne). Valeurs de DEV uniquement.
# Pour la correction : renseigner ici les identifiants admin du sujet (jamais en git).
MYSQL_ROOT_PASSWORD=change-me
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-too
JWT_SECRET=dev-secret-a-remplacer-par-une-longue-chaine-aleatoire
